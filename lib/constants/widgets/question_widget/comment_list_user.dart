import 'package:flutter/material.dart';

class UserCommentList extends StatelessWidget {
  const UserCommentList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
